---
name: Bug report
about: Report something that isn't working as expected
labels: bug
---

## Environment

- Keyart version (`keyart --version`):
- Node version (`node --version`):
- OS:
- Install method: npm install / repo clone (delete whichever doesn't apply)
- Is `OPENAI_API_KEY` set? yes / no

## What happened

**Command run** (the exact command line, including flags):

```
```

**Expected behavior:**

**Actual behavior:**

**Full error output** (paste the entire stack trace / CLI output — not just
the last line):

```
```

> **Do not paste your API key.** If the error output contains it, redact it
> before pasting.
